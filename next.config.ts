import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/maps", // Set this to your repository name
  assetPrefix: "/maps", // Set this to your repository name
};

export default nextConfig;
